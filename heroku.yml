build:
  docker:
    web: Dockerfile
    worker: Dockerfile
run:
  web: gunicorn --bind 0.0.0.0:$PORT --workers 1 --threads 2 --timeout 120 app:app
  worker: bash -c "aria2c --enable-rpc --rpc-listen-all --daemon=true && python3 main.py"
